using System;

namespace Attributes
{
    /// <summary>
    /// Attribute for methods which must be executed after every test in a class
    /// </summary>
    public class After : Attribute
    {
    }
}
